apiVersion: v1
kind: ServiceAccount
metadata:
  name: dynamo-web
  namespace: dynamo
  annotations:
    # Set this to an IAM role ARN when using EKS IRSA.
    eks.amazonaws.com/role-arn: arn:aws:iam::<account-id>:role/<dynamo-web-role>

